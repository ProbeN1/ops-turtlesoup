services:
  ops-turtle-soup:
    build:
      context: .
      args:
        RELEASE_GIT_COMMIT: ${RELEASE_GIT_COMMIT:-unknown}
        RELEASE_NAME: ${RELEASE_NAME:-docker-compose}
    env_file:
      - .env
    ports:
      - "5725:5725"
    restart: unless-stopped
